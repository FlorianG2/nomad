<!--
<%= link_to "log in", new_user_session_path %>
<%= link_to "Log out", destroy_user_session_path, class: "dropdown-item", data: {turbo_method: :delete} %>
<%= @countries[rand(0...235)].name %>
<% currency = @currencies[rand(0..118)] %>
<br>
<%= currency.name %>
(<%= currency.symbol %>)
-->
<div class="container" data-controller="progress-bar">

  <!-- Navbar -->
  <div class="d-flex align-items-center justify-content-between">
    <p id="app_name">NOMAD</p>
    <div class="d-flex">
      <div class="mx-2">
        <%= link_to "Sign Up", new_user_registration_path, id:"light-btn" %>
      </div>
      <div class="mx-2">
        <%= link_to "Log In", new_user_session_path, id:"green-btn" %>
      </div>
    </div>
  </div>
  <!-- -->

  <div class="main-container">
    <!-- Block Text -->
    <div class="left_container-landing-page">
      <p class="my-3">The easiest way to manage your <span class="limelight">budget</span> while backpacking</p>
      <%= link_to "Try it !", new_user_session_path, id:"orange-btn" %>
    </div>
    <!-- -->

    <!-- Image -->
    <div class="right_container-landing-page">
    </div>
    <!-- -->
  </div>

  <!-- Progress Bar -->
    <div class="footer-progress">
      <div class="progress-container">
        <div class="progress-bar" id="progressBar">
        </div>
      </div>
    </div>
  <!-- -->
</div>
